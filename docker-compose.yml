version: "3.8"
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    env_file: ./backend/.env
    depends_on:
      - ml-engine

  ml-engine:
    build: ./ml-engine
    ports:
      - "8000:8000"
