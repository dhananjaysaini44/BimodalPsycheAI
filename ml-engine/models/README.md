# Model Artifacts

Trained model weights should be placed in this directory for local serving.
For security and size reasons, files in this directory (except this README.md) are gitignored.

Expected models:
- text classifier (e.g., `text_classifier.pt`)
- voice classifier (e.g., `voice_classifier.pt`)
